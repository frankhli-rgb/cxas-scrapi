name: CES Gemmaclaw CI

on:
  push:
    branches: [ main, feat-cesgemmaclaw ]
    paths:
      - 'gemmaclaw/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'gemmaclaw/**'

jobs:
  test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: gemmaclaw

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: gemmaclaw/package-lock.json

      - name: Install dependencies
        run: npm install

      - name: Build CLI
        run: npm run build

      - name: Run Unit Tests
        run: npm run test
