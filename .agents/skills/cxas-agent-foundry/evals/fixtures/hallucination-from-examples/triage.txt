Eval type: sim
Flow: mobile_data_troubleshooting
Result: 1/5 FAIL (grounding)

Failure detail (runs 1, 2, 4, 5):
  Grounding check FAILED — agent suggested a step not present in
  the KB tool response for this session.

  KB tool returned:
    - "Check if airplane mode is enabled"
    - "Toggle mobile data off and on"

  Agent response included:
    - "Check if airplane mode is enabled"    [GROUNDED - pass]
    - "Toggle mobile data off and on"        [GROUNDED - pass]
    - "Try restarting your Wi-Fi router"     [NOT GROUNDED - fail]

The phrase "Try restarting your Wi-Fi router" appears in the
instruction's example block (example_response section, line 34)
but was NOT returned by the kb_search tool for this query.

The model is pulling from the static example text in the prompt
instead of grounding exclusively on the dynamic tool output.

Fix: remove or minimize inline examples, or add an explicit
instruction: "Only suggest steps returned by the KB tool."
