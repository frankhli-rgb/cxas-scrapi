Eval type: golden
Flow: escalation_profanity
Result: 0/5 FAIL

All 5 runs show identical failure:
  Expected tool call: escalate_to_human(reason="abusive_language")
  Actual tool calls: NONE (TOOL_MISSING)
  Agent response: "I cannot process that request. Please rephrase."

Root cause hypothesis:
  The model's safety layer is intercepting the hostile input BEFORE
  the before_model_callback runs. The agent never reaches the
  taskflow logic — it emits a canned refusal instead of following
  the escalation step defined in the instruction.

  The before_model_callback (which would set escalation_required=true)
  never fires because the guardrail short-circuits the turn.

Relevant config:
  safety_filter: enabled (default)
  escalation_taskflow: step 3 — "If profanity detected, call escalate_to_human"
