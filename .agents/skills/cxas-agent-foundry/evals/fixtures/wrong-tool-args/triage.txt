Triage Results — telecom-support (run 2024-12-17T09:15:00Z)

FAILED: golden-update-payload
  Tool 'update_subscriber' called with wrong argument values:
    Expected: update_subscriber(msisdn="5551234567", plan="premium_50")
    Actual:   update_subscriber(msisdn="the customer", plan="upgrade plan")

  Agent used natural language values instead of extracted data.
  Tool docstring says: update_subscriber(msisdn, plan)
  No (REQUIRED) markers. No fallback to session state.

  Transcript snippet:
    User: "I want to upgrade to the premium 50 plan"
    Agent: [calls update_subscriber with msisdn="the customer", plan="upgrade plan"]
    Tool response: ERROR — invalid msisdn format

Pass rate: 80% (this is the only tool-arg failure)
