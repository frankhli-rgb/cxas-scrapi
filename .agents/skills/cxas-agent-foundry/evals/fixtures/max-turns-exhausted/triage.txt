Eval type: sim (audio mode)
Flow: account_verification_and_topup
Result: 0/5 FAIL

All 5 runs terminated with: "max_turns exceeded (8/8)"

Turn breakdown (typical run):
  Turn 1: agent greeting
  Turn 2: user states request ("I want to buy a data bundle")
  Turn 3: agent asks for ID verification
  Turn 4: user provides ID number
  Turn 5: tool_call verify_identity — success
  Turn 6: agent asks which bundle
  Turn 7: user picks "1GB for 30 days"
  Turn 8: tool_call get_bundle_options — response returned
  -- MAX TURNS (8) REACHED -- session killed before agent can
     call purchase_bundle or confirm with the customer

The flow requires at minimum 10-11 turns in audio mode:
  greeting + request + verify + confirm_id + select_bundle +
  confirm_bundle + purchase + confirmation + goodbye

In text mode this flow passes because the model can bundle
multiple actions into fewer turns. Audio mode forces strict
turn-taking (one utterance = one turn).

Config: max_turns = 8 (set globally, not per-flow)

Fix: increase max_turns to 15 for audio evals, or set per-flow
limits. The verification step alone costs 3-4 turns in audio.
